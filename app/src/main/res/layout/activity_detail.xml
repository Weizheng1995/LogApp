<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.wz.logapp.DetailActivity">
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:layout_marginLeft="80dp"
        android:layout_marginStart="80dp"
        android:id="@+id/detailTitle"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="66dp"
        android:hint="标题" />

    <Button
        android:text="更新"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/detailContent"
        android:layout_below="@+id/detailContent"
        android:layout_marginTop="20dp"
        android:id="@+id/btnUpdate" />
    <Button
        android:text="删除"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/detailContent"
        android:layout_below="@+id/detailContent"
        android:layout_marginTop="20dp"
        android:id="@+id/btnDele" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:enabled="true"
        android:gravity="top"
        android:scrollbars="vertical"
        android:minLines="10"
        android:maxLines="10"
        android:layout_marginTop="46dp"
        android:id="@+id/detailContent"
        android:hint="内容"
        android:layout_below="@+id/detailTitle"
        android:layout_alignLeft="@+id/detailTitle"
        android:layout_alignStart="@+id/detailTitle" />
</RelativeLayout>
